# ROADMAP

按价值排序的待办。每条都对应 README「Known limitations」里的一项。

---

## ✅ 1. 跨案例验证（2026-09-18 完成第一轮）

**做了什么**：拿整套流程去实拆一个此前没碰过的 App（Pocket，硬件录音笔的 Flutter 客户端，
真机冷启动 + 稳态各录一段），并把三个脚本跑在另外五个**公开发行**的 App 安装包上
（Transkriptor / 有道 GetNotes / Notion / X / 抖音），覆盖 Flutter 与原生、规范命名与混淆命名。

**结果：找到 6 个 bug，全部已修，并被 `scripts/test_regressions.py` 钉住**（逐条见 `references/pitfalls.md` 9–14）：

| # | bug | 影响面 |
|---|---|---|
| 9 | `ble` 子串命中 `drawable`/`variable`/`double`/`cable` | **六个 App 全中**；X 是 2886 个资源里 1976 个 |
| 10 | `--contrast` 在截图上把文字自己当成背景极值 | 结论完全反了（`#111111` 判 FAIL 1.06） |
| 11 | 没有循环也报「周期」 | 一次性启动序列被报成 `cycle 1.60s` |
| 12 | 起录太晚，入场动效已经演完 | 差点把有动效的屏判成纯静态 |
| 13 | `ffprobe` 的 `nb_frames` 在 adb 录屏上是假的 | 5.87 秒 117 帧的片子报「5 帧」 |
| 14 | 屏幕桶当成了资源清单用 | 真正的设备资源掉出桶外 |

**六条里五条是脚本自己报错，不是判断失误** —— 说明「从一次调研里提炼的工具」
最先失效的地方是**换一个命名风格的 App**，而不是换一个分析对象。

**这一轮没覆盖到的**（⛔ 不要当成已验）：

- `SKILL.md` 五步流程在「**没有真机**」「**只有网页**」「**iOS 产品**」三种情况下够不够用 —— `SKIP(未验)`
- 只拆了 sign-in 一屏；付费墙 / 列表 / 空状态的流程没走过
- 只在一台设备、一个 Android 版本上跑过

⇒ **下一轮换 iOS 或网页产品做**，那是现在唯一完全没有证据的方向（见第 3 项）。

---

## ✅ 9. C 档「功能调研」（2026-09-21）

用户提一个要做的功能 → 授权后真机调研 + 解包取证 → 产出需求分析 / 功能分析 / 技术剖解。
硬要求是「**必须精准且不能有误**」，所以核心不是流程而是**证据纪律**。

**新增 `scripts/feature_probe.py`**（纯标准库）：从 APK 抽四类证据，每条标来源文件——
原生库（端侧能力）、包名前缀命中（第三方栈）、域名（云端依赖）、接口路径（功能模块地图）。
dex 字符串表和 `.so`/RN bundle 的可打印串都自己解析，不依赖任何外部工具。

**证据标签制度**：`[真机]` 亲眼看到 / `[包内]` 追溯到具体文件 / `[推断]` 证据撑不住这句话。
⭐ 架构类结论（端侧还是云端）**默认 PLAUSIBLE**，哪怕两个标签都有。

### Codex 评审：两轮，第二轮推翻了我「全部已修」的说法

⛔ **首轮结论是「不建议合并」**，而且抓到的问题我自己没看出来：

| # | 问题 | 修法 |
|---|---|---|
| 1 | dex 偏移指向 header 时**静默返回错误字符串**（偏移 0 → `ex\n`）；损坏条目静默跳过，和「没命中」分不开 | 偏移做数据区边界检查；跳过条目**报数** |
| 2 | ⭐ **§C.4 模板自己就是反例**：「无端侧库 + 飞行模式不可用 ⇒ 识别在云端」——本地识别一样可能联网校验。`[推断]` 定义成「两者都没有」也错了：**有证据但撑不住结论仍是推断** | 重写标签定义与示范；架构结论一律 PLAUSIBLE |
| 3 | 接口正则把资源路径 `/assets/v1/icons/logo.png` 认成接口、漏 `/v12/`、长路径被**截断成不存在的路径** | 整条路径锚定匹配，宁缺毋滥 |
| 4 | 「2309 个类」是代码支撑不了的测量（数的是字符串命中，没读 `class_defs`） | 改称「包名前缀命中」并标注 |
| 5 | 文档宣称覆盖 RN，`collect()` 根本不读 JS bundle | 实际读 `assets/*.bundle` |
| 6 | ⭐ **公开仓库的 pitfalls 里贴了竞品真实域名、S3 桶和业务接口路径**——而脚本第 17 行写着「⛔ 不要发布接口清单」 | 全部脱敏，示例改用 `/api/v1/<模块>` |
| 7 | 新增测试**没测真实行为**：把 `report_*` 换成空函数，四个测试照样全绿 | 改为调用真函数断言输出；6/6 空实现变红 |

⚠️ **第二轮复审判定「2 项已修、8 项部分修」，并抓到一个新的阻断问题** ——
我上一轮说「7 条全部已修」是不成立的，这行记录本身就是个教训：
**「我改过了」和「改对了」是两件事，只有对手方重做变异才算数。** 第二轮补修：

| 问题 | 实情 |
|---|---|
| ⭐ **新 must_fix**：`https://[broken` 让 `urlsplit` 抛 `ValueError`，**整次分析中断**，后面的有效证据全丢 | 已捕获并跳过；补了回归样本 |
| dex 仍信任文件自报的 `header_size` | 构造 `header_size=1` 能让它返回文件魔数当应用数据。改成按 dex 规范固定校验 `0x70`，并拒绝指向 string_ids 表的偏移 |
| 接口正则改锚定后**漏掉真实形态** | `?limit=10`、`/{id}`、`getUser` 全漏；压缩成一行的 JS bundle 里的接口也漏。已支持，且 `none matched` 时明说覆盖范围，⛔ 不再让漏报看起来像「没有接口」 |
| 关键词报告把 >200 字符的串**整条丢掉** | 压缩 bundle 是一整行，命中全没了。改成截窗口输出 |
| 「RevenueCat 2309 个类」仍写在 pitfalls 里 | 那是字符串命中数不是类定义数，已改口径 |
| SKILL 仍写「只有 libavcodec ⇒ 识别在云上」 | 与刚立的证据纪律自相矛盾，已改成「具备本地转码条件」+ 明确这是 `[推断]` |
| `report_keyword` / `report_sdk` 清空后测试**仍全绿** | 测试只验了标题没验内容。已补；现在 **10 个变异 10 个变红** |

**第三轮**：旧反例全部实证修复，但我**新引入**了一个——为支持压缩长行加的「扫描内嵌 URL」
把 `/api/v1/users,archive` 截断成不存在的 `/api/v1/users`；且 dex 的两处校验**撤掉后测试仍全绿**（没钉住）。
均已修，并补了 `pitfalls.md` 第 20–22 条。⚠️ 第 22 条是「变异没打中目标行」**在本项目第三次出现**。

**第四、五轮**：又抓到三处，**全部是我在修上一轮时新引入的**——
诊断不带来源（多 split 时无法归属「未覆盖」）、
`rstrip` 把代码里真实路径的尾部标点删掉伪造端点、
以及那个判断用的是**排除法**（「后面不是引号 ⇒ 是散文」），
URL 落在字符串末尾或被反斜杠截断时两种都「不是引号」，照样伪造。
改成只认正面证据（标点后面确实跟着空白）。⭐ 教训记在 `pitfalls.md` 第 21 条：
**「不是 X」在开放集合上不能当作「是 Y」的证据。**

**第六轮**又抓到一个同族但**不依赖尾部标点**的变体：正则在反斜杠转义处停住，
留下的 `/api/v1/users` 不需要任何裁剪就干净地通过了检查，而真实路径是 `/api/v1/usersarchive`。
⇒ 一开始是逐个拒绝截断候选。同时补了两处**正断言**（`/api/v1/users.` 必须原样保留）和
一条**被接受的召回退化**样本（引号内的散文标点宁可拒绝也不猜）。

**第七轮**给出第五、六种形态（双引号内的单引号、非 ASCII 字节在更上游截断）之后，
我停止打补丁，**删掉了整条「从长文本抠内嵌 URL」的扫描** ——
六轮六种假接口是**同一个** bug：从可能被截断的文本里抠出的前缀不能当完整路径。
现在只认「整条完整字符串本身就是路径」，并给每条字符串标记是否被上游截断。
⚠️ 删除的代价也量过：第一版完整性判据（「NUL 结尾才算完整」）把 Pocket 各 `.so` 合计
**111738** 条判成碎片、路径候选归零；改判据后降到 152 条、候选恢复 127 条
（⚠️ 152 是**启发式拒绝**，评审已找出其中误判，⛔ 不等于都是真碎片）。

**第八轮**证明「删掉就干净了」是错觉——又找出三个**不经过那条扫描**的伪造入口：
碎片判据只查右边界（左侧截断的 `中文/api/v1/users` 照样通过）、
定长窗口解码失败时反而放行（`usersé中archive`、`users\xc3\xa9\x00\xff`、NBSP 三种）、
以及 `candidate_path` **无条件剥引号**（`…/api/v1/users'` 的引号是路径的一部分）。
均已修：按 UTF-8 首字节定长解码、左右边界都查、不再剥引号。

**第九轮**又是三条，其中两条是我自己的判据出问题：
`candidate_path` 的 `.strip()` 仍在改写完整字符串（`\u00a0/api/v1/users` 被削成 `/api/v1/users`），
而二进制侧刚刚才正确地把 U+00A0 认作真实字符——**同一个字符，两条路径两种结论**；
`urlsplit` 还会静默删掉路径里的 TAB/CR/LF。现在**在解析之前先校验原始字符串**——含控制字符或首尾空白一律拒绝，不 strip、不剥引号。
（⚠️ 第十轮证明只拦 TAB/CR/LF 不够：`urlsplit` 在更下层还会吞掉前导空格和 `\x01`。）
第三条是右边界的三/四字节没有回归保护（只有 2 字节的 `é` 样本，把解码缩回定长 2 字节仍全绿）。

⚠️ 并且第九轮**推翻了我上一轮的一个事实陈述**：被左边界挡掉的那 2 条，
我说「前置字节确实是阿拉伯字母」，实际是**跨记录拼出来的**（上一条字符串的尾字节 + 当前记录的长度字节）。
⇒ 那 2 条是完整记录，是**误杀**。输出口径已改成「启发式拒绝、会误杀」。

**当前：25 个回归样本，7 个关键变异全红。**
⚠️ ⛔ 不要读成「这一类已经解决」—— 九轮里每一轮都找到了新形态，
而且从第三轮起，新问题大多是**上一轮修复引入的**。

should_fix 也一并修了：`user@host` 被解析成 host `user`（改用 `urlsplit`）、
来源标签丢失 ABI 路径、`adb logcat -c` **会清空整台设备的日志**（改 `-T 1`）、
真机调研加了额度与停止条件、三个样本不再推广成平台定律（补了 feature split 的覆盖缺口）。

**触发评测**：调参集 36 条 **35/36**，唯一失败的是「**这块**的导出功能到底支持到什么程度」——
「这块」既可能指竞品也可能指自家代码，**题目本身有歧义**，不触发是合理行为，
⛔ 没有为它调 description。留出集 12 条 10/12，两条失败同一形态（口语化追问实现细节），
已补进 description 的第 ⑥ 类；⚠️ **留出集对这一类已被污染，下轮需要全新 query**。

---

## 🔄 8. 定位升级：从「拆解引擎」到「拆解 + 找思路」（2026-09-19 开始）

**背景**：扫了 GitHub，没有一个项目是「web + App 都覆盖、实测而非看图、目的是帮用户分析后自己拍板」的——
定位像的（ux-scout、ux-lens）靠视觉模型看截图没有测量；有测量的（web-to-design-md、site-teardown）只做 web
且目标是复刻。这个位置是空的，但本仓库此前只做了「拆这个」一个入口，「帮我找思路」基本没做。

**2026-09-20 追加**：修掉一处**自相矛盾的边界** —— 边界表写「下载安装包 ⚠️ 先征求同意」，
第 3 步却写「设备上没有的请用户自己装」，等于设备上没装的产品整个 App 端都拆不了，
而这正是「帮我找思路」最常见的场景（候选产品本来就不在你手机上）。
改成三档：设备已装 `adb pull` > 经授权从公开渠道下载免费包（**必须核 versionCode**） >
⛔ 驱动用户账号 / 付费应用 / 绕地区限制。配套加了 `pitfalls.md` 第 15、16 条与 preflight 的 aapt 检查。

**已做（2026-09-19，未重新跑触发评测）**：
- `SKILL.md` 第 1 步改成两个入口：**A 拆这个** / **B 帮我找**；B 模式补了 web + App 的候选来源
  （商店 API 直接取截图筛人、设计奖项站、截图库），并明确「筛到三个就停、web 与 App 混合」
- 第 5 步加了 B 模式的产出模板：**对比简报**（三个参考 → 共同点 / 分歧点 → 结合用户约束的建议），
  不是单屏规格；截图库来的一律记 SKIP
- description 与 `AGENTS.md` 写明两个入口

**未做**：
- [x] ⭐ **C 档真机部分 2026-09-21 首次实跑**（有真机），一次就撞出三个**流程级**缺口，全部已补进 SKILL.md §C.2 与 `pitfalls.md` 25–27（§C.2 已于 2026-09-22 移入 `references/feature-research.md`）：
  - **登录墙**：目标功能在登录后面，按纪律不注册/不动用户账号 ⇒ 真机路径整条作废。
    而 C.2 清单默认「你已经在 App 里」，**没有「够不够得着」这个前置**。现已补「先判可达性」一节
  - **Flutter 应用 `uiautomator dump` 读不到文本**（实测 Flutter 0 个节点 vs 非 Flutter 16 个）——
    而这个品类竞品大多是 Flutter。现已补「先判运行时再定遍历方式」
  - **我自己违反了刚写的 `logcat -c` 禁令** ⇒ 禁令必须配一条可直接抄的正确命令
  ⚠️ 仍未走完整轮：没有产出过一份完整的功能调研报告（卡在可达性）
- [ ] B 模式**一次都没实跑过**——拿一个真实的「我没思路」场景走一遍（最好是用户自己产品正在纠结的页面）
- [x] 触发评测：评测集加了 6 条 B 类（3 应触发 + 3 近似负例），第一次 **25/26**——原 20 条全保持、中文 B 类 3/3、误触发 0；唯一漏的是英文 B 类，description 补了一条英文 B 例后复测见下一行
- [x] 复测（description 补一条英文 B 例之后）：**26/26**。⚠️ 这是调参集，英文那条正是因为失败才补进 description 的，属于拿自己的卷子考自己；**留出集 8 条里一条 B 类都没有，B 模式的泛化能力等于没测**
- [ ] web 端补到 App 端同一水平：`scripts/web_probe.js`（贴进任何浏览器工具就吐 JSON 规格）+ 在真实网站实跑 +
  把 `getAnimations()` 未触发、跨域样式表抛异常这些坑量出来写进 pitfalls
- [ ] 双端统一三态输出格式，报告从数据生成、可被检查（回应第 1 项里「第 5 步无机制保障」）
- [x] 改名：`competitor-ui-teardown` → **`how-they-built-it`**（2026-09-22，用户拍板）。
  旧名两个词都是窄的：`competitor` 挡掉「帮我找思路」入口，`ui` 挡掉「功能调研」入口；
  新名是用户原话，三个入口问的都是同一句。SKILL.md 的 `name`、目录、本机软链、
  README/ROADMAP 内所有引用已同步。⚠️ **GitHub 上的仓库重命名还没做** ——
  README 里的 clone URL 要等它改完才有效，顺序是：先在 GitHub 改名，再 push
- [x] 工具预检：`scripts/preflight.py`——ffmpeg / ffprobe / adb / 设备四项，缺的按平台给安装命令，退出码区分「能不能做 App 拆解」（2026-09-19）

### 2026-09-22 这一轮新开的口子

- [ ] **新留出集没跑**：已起草 20 条全新查询（`evals/trigger-eval-holdout-2.json`，10 应触发 / 10 近似负例），刻意避开调参集已覆盖的形状，
  新增了 web 产品、设计师口吻两类。最有价值的一条负例是「这个开源库的虚拟滚动怎么实现的，帮我读源码」——
  它和触发条件高度重合（追问机制），但**有源码时逆向是浪费**，而 description 目前没有任何一句排除它。
  下一步：`python3 -m scripts.run_eval`（skill-creator）对当前 description 打分，每条跑 3 次
- [x] **渐进披露**（2026-09-22）：§C（220 行）移到 `references/feature-research.md`，SKILL.md **508 → 306 行**。
  ⛔ 证据标签制度没有一起搬走 —— 它是防编造的闸，SKILL.md 里留了三条浓缩规则（必须挂标签／`[包内]`≠功能存在／架构结论默认推断），
  读不到 reference 也拦得住
- [x] `docs/*.svg` 的可复现性（2026-09-22）：`tools/make_figures.sh` 一条命令重建全部 fixture 和三张图，
  CONTRIBUTING 加了一节。实测重跑三张图的结论逐字一致（光斑 25%/26%、对比度 12.40/2.73、cycle 4.16s）
- [ ] `frame_diff.py` 的 `--res` 告警和第 30 条的采样率告警是两套独立启发式，
  低幅运动时可能同时触发、互相矛盾。没实测过两者叠加的情况

---

## ✅ 2. 触发准确性评测（2026-09-18 完成第一轮）

评测集在 `evals/`：`trigger-eval.json`（20 条，调参用）+ `trigger-eval-holdout.json`（8 条，**独立验证用**）。

| 轮次 | 调参集 20 条 | 留出集 8 条 |
|---|---|---|
| 原 description | **15/20**（误触发 0/10，应触发只中 5） | — |
| 改写后 | **20/20** | **7/8** |

⭐ **看留出集那个数，不要看 20/20** —— 评测集和 description 都是我自己写的，
20/20 是拿它自己的卷子考自己。7/8 才是泛化估计。

漏的那条是「他们那个录音波形是自己画的还是用的现成库？」——「**问实现来源**」这个形态
（不是问视觉）没覆盖到。⛔ 没有继续调，再调就把留出集也污染了；下一轮要配一批**新的**留出 query。

**原 description 漏的五条集中在三类**，改写就是照着补的：
①没点名具体产品（「同类产品」「对手」）②只问一个机制细节（「是视频还是静态图」「多长时间」）③英文。

### ⛔ 跑评测之前必看：已安装的 skill 会被判 0 触发

`run_eval.py` 把被测 description 写成 `~/.claude/commands/<name>-skill-<uuid>.md`，
**只有模型调用的名字里含那个 uuid 后缀才算触发**。
本技能已经软链进 `~/.claude/skills/`，模型调的是**真名**，uuid 副本永远匹配不上
⇒ 第一次跑出来 **20 条全 0**，退出码 0、不报任何警告，差点被当成「description 写得差」。

抓 `--output-format stream-json` 看一眼就清楚了：第一个 tool 就是 `Skill{skill: how-they-built-it}`，
**实际触发了**。修法是拿 `run_eval.py` 的副本把三处 `clean_name in ...` 换成 `skill_name in ...`
（stream 事件两处 + assistant 消息一处）：

```bash
cp -R ~/.claude/skills/skill-creator/scripts /tmp/evalrun/ && cd /tmp/evalrun
# 改掉上面三处之后：
python3 -m scripts.run_eval --eval-set ~/how-they-built-it/evals/trigger-eval.json \
  --skill-path ~/how-they-built-it --model <model id> --runs-per-query 3 --verbose
```

⚠️ 它还有一条：**第一个 tool 不是 `Skill`/`Read` 就直接判 false**，先跑 Bash 探环境的会被误判。

---

## 3. iOS 支持 —— ⏸ 暂不做（明确的非目标）

**iPhone 上没有 adb**，整条「连设备 → 列包 → 拉安装包 → 读资源清单」的链路在 iOS 上都不成立，
而资源清单正是本流程里最决定性的一步。投入产出不划算，**暂时不支持**。

已经做的是**把边界说清楚**，避免用户撞上去（`SKILL.md` 第 2 步上方、README「Known limitations」）：
iOS 上「怎么动」和「什么颜色」仍然能量（录屏 → `frame_diff.py`，截图 → `image_probe.py`），
**「用什么做的」量不了** —— 报告里这一条必须记 `SKIP(未验)`，⛔ 不许写成实测。

真要重开这一项，先回答这两个问题，别直接上手：
- 拿 IPA 的路子（`ipatool` 一类）要登录用户自己的 Apple ID，**是否合规、是否愿意**由用户决定，⛔ 不替用户操作商店账号
- 不解包的话，能不能靠「录屏 + 截图」把结论做到可用？做不到就不值得做

---

## ✅ 4. 自动推荐 --crop（2026-09-18 完成）

`frame_diff.py --suggest-crop`：逐格算整段录屏的 max−min，取「变过的格子」的外接矩形。

实测三种输入都给了有用的答案：

| 输入 | 输出 |
|---|---|
| 静止屏录满 16.8 秒 | `--crop 30x20+850+60` + ⚠️ **这是状态栏里的一条，八成是时钟/电量** —— 正好是脚本自己警告过的那个坑 |
| 含整屏硬切的冷启动 | 整帧 + ⚠️ **片子里有换屏，先剪成一屏再跑** |
| 剪掉换屏之后的尾段 | `nothing moved` |

⛔ 它给的是**外接矩形不是掩码**：两个分开的动效角会得到一个把中间死区也圈进去的大框，
自己拆。结论一律**回画面确认**再用。

---

## ~~5. 区域对比度~~ —— 已否决，原因与预想的相反

原计划：给 `image_probe.py --contrast` 加 `--region WxH+X+Y`，理由是全图极值对
「文字只出现在某一块」的情况过于悲观。

**2026-09-18 实测推翻了这个方案**：真正的毛病不是范围太大，而是**极值本身取到了文字像素**。
把范围裁到文字带里，文字占比反而更高，结论**更错**（`#8A8A8E` 从 3.44 掉到 1.67）。

已改成取网格的 **p10/p90** 而不是绝对极值，并在输出里同时印出绝对极值供对照；
真正的解法是**喂解包出来的背景素材，不喂截图**。见 `pitfalls.md` 第 10 条。

---

## ~~6. split APK~~ —— 降级为低优先，base.apk 实测够用

原计划：`pm path` 返回多个 apk 时自动合并多个包的资源清单。

**实测六个 App**（Pocket / Transkriptor / GetNotes / Notion / X / 抖音）：
`base.apk` 装了资源体积的 **99% 以上**，density 与语言 split 加起来只有 0.1MB 级别的零头，
且**没有一个超过 20KB 下限**。⇒ 合并清单换不来结论上的差别。

剩下的只是手感问题：`pm path` 会返回五六行让人挑（10 个 App 里 9 个是 split）。
真要做就做成「接受多个 apk 路径」，⛔ 别做成自动下载与合并。

---

## ✅ 7. 回归样本（2026-09-18 完成）

`scripts/test_regressions.py`：**8 个已知 bug 每个一条样本**，
`pitfalls.md` 第 7、8 条（阈值写死 / HLS 饱和度劫持）与第 9–14 条（跨案例验证找到的六个）全覆盖。

**逐条做过 mutation 验证**：把每个修复单独改回去，**9/9 都只让对应的那一条变红**。
⚠️ 中途抓到一次「mutation 没打中目标行、测试照样全绿」——
所以 mutation 脚本自己也要 `assert old in s`，确认**真的改到了**再看结果。

```bash
python3 scripts/test_regressions.py      # 无依赖，1 秒跑完
```
